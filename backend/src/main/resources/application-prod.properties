# Production Environment Configuration Template
spring.application.name=crud

# Database Configuration
# TODO: Replace with production database credentials
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/crud_db}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:}
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.web.resources.static-locations=file:/var/uploads/

# Logging
logging.level.com.example.crud=INFO
logging.level.org.springframework.security=WARN

# Security
server.ssl.enabled=false
# TODO: Enable SSL for production
# server.ssl.key-store=classpath:keystore.p12
# server.ssl.key-store-password=${SSL_PASSWORD}
# server.ssl.key-store-type=PKCS12
